name: "ActionUsingUploadArtifactAction"
on: [push]
jobs:
  SomeJob:
    steps:
      - name:
        uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: path/to/artifact/file.txt
